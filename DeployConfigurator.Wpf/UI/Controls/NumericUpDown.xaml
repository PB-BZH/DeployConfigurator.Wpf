<UserControl x:Class="DeployConfigurator.Wpf.UI.Controls.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DeployConfigurator.Wpf.UI.Controls"
             mc:Ignorable="d"
             d:DesignHeight="25"
             d:DesignWidth="150">
    <Grid Width="150"
          Height="25">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="22" />
        </Grid.ColumnDefinitions>

        <TextBox x:Name="txtValue"
                 Grid.Column="0"
                 VerticalContentAlignment="Center"
                 HorizontalContentAlignment="Right"
                 Padding="4,0"
                 LostKeyboardFocus="txtValue_LostKeyboardFocus"
                 KeyDown="txtValue_KeyDown" />
        
        <Grid Grid.Column="1">

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button x:Name="btnUp"
                    Grid.Row="0"
                    Content="▲"
                    Padding="0"
                    FontSize="8"
                    Click="btnUp_Click" />

            <Button x:Name="btnDown"
                    Grid.Row="1"
                    Content="▼"
                    Padding="0"
                    FontSize="8"
                    Click="btnDown_Click" />

        </Grid>

    </Grid>
</UserControl>
